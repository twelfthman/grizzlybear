require 'test_helper'

class EventsControllerTest < ActionController::TestCase
  describe EventsController do
    describe "search action" do
      skip "write the search action"

      it "sets the @event_type variable to an EventType"
      it "searches for events near location"
      it "sets @events to the the events near the location"
    end
  end
end
